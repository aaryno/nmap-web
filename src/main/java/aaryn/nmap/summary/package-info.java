/**
 * Plain Java beans (POJOs) for consolidating single scans and scan histories
 * into JSON, plus the factories that build the POJOs from NmapScan hibernate
 * entities.
 */
/**
 * @author aaryno1
 *
 */
package aaryn.nmap.summary;